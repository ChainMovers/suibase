import{_ as e}from"./plugin-vue_export-helper-c27b6911.js";import{o as t,c as i,e as n}from"./app-ddb948df.js";const s={},a=n(`<h1 id="workdir-conventions" tabindex="-1"><a class="header-anchor" href="#workdir-conventions" aria-hidden="true">#</a> Workdir Conventions</h1><p>Suibase define a few conventions to coordinate among SDKs, apps and user.</p><div class="hint-container tip"><p class="hint-container-title">Tips</p><p>This section present more advanced subjects. If just starting to use Suibase scripts, then you may want to skip it.</p></div><h2 id="filesystem-path-convention" tabindex="-1"><a class="header-anchor" href="#filesystem-path-convention" aria-hidden="true">#</a> Filesystem Path Convention</h2><p>There are 6 &lt;WORKDIR&gt;: <code>active</code>, <code>localnet</code>, <code>devnet</code>, <code>testnet</code>, <code>mainnet</code> and <code>cargobin</code></p><p>Each &lt;WORKDIR&gt; has the following components:</p><table><thead><tr><th>Component</th><th>Purpose</th></tr></thead><tbody><tr><td>sui-exec</td><td>A script allowing any app to safely call the right sui client+config combination for this workdir. Use it like you would use the &quot;sui&quot; client from Mysten Lab.</td></tr><tr><td>config</td><td>Directory with Mysten Lab files needed to run the sui client (client.yaml and sui.keystore).</td></tr><tr><td>sui-repo</td><td>A local repo of the Mysten lab sui code for building the client binary, but also for any apps to use the Rust SDK crates for compatibility.</td></tr><tr><td>published-data</td><td>Information about last package published from this &lt;WORKDIR&gt; using suibase scripts. This can be retrieved through JSON files or through suibase SDK helpers.</td></tr><tr><td>workdir-exec</td><td>A script allowing any app to safely call the right &quot;workdir script&quot;.<br> Example: <code>$ ~/workdirs/localnet/workdir-exec update</code> is equivalent to the shortcut <code>$ localnet update</code></td></tr></tbody></table><p>Applications can expect the components to be always at these <strong>fix</strong> locations:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code> ~/suibase/
     └─ workdirs/
          └─ &lt;WORKDIR&gt;/
                ├── sui-exec
                ├── workdir-exec
                │
                ├── config
                │      ├── client.yaml
                │      └── sui.keystore
                │
                ├── sui-repo
                │      ├── crates/
                │      ├── target/
                │      └── ... complete sui repo (debug built) ...
                │
                └── published-data
                       └─ &lt;package name&gt;
                               └─ publish-output.json
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><details class="hint-container details"><summary>Official and Complete Path List</summary><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>    ~/suibase/workdirs/&lt;WORKDIR&gt;/sui-exec
    ~/suibase/workdirs/&lt;WORKDIR&gt;/workdir-exec
    ~/suibase/workdirs/&lt;WORKDIR&gt;/config/client.yaml
    ~/suibase/workdirs/&lt;WORKDIR&gt;/config/sui.keystore
    ~/suibase/workdirs/&lt;WORKDIR&gt;/sui-repo/
    ~/suibase/workdirs/&lt;WORKDIR&gt;/published-data/&lt;PACKAGE_NAME&gt;/publish-output.json
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h2 id="what-is-the-publish-output-json" tabindex="-1"><a class="header-anchor" href="#what-is-the-publish-output-json" aria-hidden="true">#</a> What is the publish-output.json?</h2><p>Doing publish using suibase scripts will create a publish-output.json file in the published-data directory of the workdir.</p><p>This file can then conveniently be read by your app (through Suibase helpers) to get package_id and object_id of new shared object published.</p><h2 id="concurrency-limitation" tabindex="-1"><a class="header-anchor" href="#concurrency-limitation" aria-hidden="true">#</a> Concurrency Limitation</h2><p>When attempting to do many things at once (multiple apps accessing multiple network at same time), a few rules to keep in mind:</p><ul><li><p>In the context of Suibase, each Sui client is associated with a single network (localnet, devnet, testnet, mainnet). You should NEVER edit the client.yaml to target different network type (localnet vs testnet). It is ok to have multiple env to target different RPC toward the <strong>same</strong> network (e.g. <code>fullnode.testnet.sui.io</code>, <code>fullnode.testnet.vincagame.com</code> ...)</p></li><li><p>Each Sui client have a single active address. An app changing the active-address should not assume that it will remain unchanged (because the user or other app are allowed to change it also). Suibase helper provides alternative to get client addresses in a more deterministic way (e.g. by name) for advanced test setup.</p></li><li><p>Changing the active workdir for &quot;asui&quot; can be safely done from multiple apps (and API), but only the last call will be effective. Changing the active workdir is not to be done &quot;lightly&quot;, and typically it is expected that it will be driven by one user on the CLI (e.g. &quot;localnet set-active&quot;).</p></li></ul><div class="hint-container warning"><p class="hint-container-title">Note</p><p>When the active workdir of &quot;asui&quot; is changed, it is recommended to &quot;cargo clean&quot; or &quot;rebuild&quot; your dependent apps. This is to make sure that the new workdir context is re-applied to all tools/SDKs.</p></div>`,17),o=[a];function d(r,l){return t(),i("div",null,o)}const h=e(s,[["render",d],["__file","references.html.vue"]]);export{h as default};
