{
  "name": "walrus-sdk-tests",
  "version": "1.0.0",
  "description": "TypeScript tests for Walrus SDK integration with Suibase upload relay",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node dist/upload-test.js",
    "test:basic": "npm run build && node dist/basic-relay-test.js",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "@mysten/sui": "^1.13.0",
    "@mysten/walrus": "^0.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "Apache-2.0"
}